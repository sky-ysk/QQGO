# v0.7 测试报告

**日期:** 2026-05-26
**分支:** feature/v0.7
**测试执行时间:** 2026-05-26 16:58

---

## 构建状态

| 项目 | 结果 |
|------|------|
| `go build ./...` | ✅ PASS |
| `go test ./internal/service/` | ✅ 19/19 PASS (2.704s) |
| `go test ./cmd/client/` | ✅ 1/1 PASS |

---

## v0.7 需求完成总览

| # | 需求 | 类型 | 状态 | 测试 |
|---|------|------|------|------|
| 0 | Server 优雅退出 | Bugfix | ✅ | 手动验证端口释放 |
| 1 | SQLite GREATEST 兼容 | Bugfix | ✅ | TestGroupChat 无报错 |
| 2 | BUG-005: 好友上限 500 | Test | ✅ | TestFriendLimit500 |
| 3 | BUG-006: 离线好友请求 | Test | ✅ | TestOfflineFriendRequest |
| 4 | 修改密码 | Feature | ✅ | TestChangePassword, TestChangePasswordWrongOld |
| 5 | 黑名单 | Feature | ✅ | TestBlockUser, TestBlockSelf, TestUnblockUser, TestGetBlacklist |
| 6 | 图片/文件消息 | Feature | ✅ | Handler 已有, 客户端命令新增 |
| 7 | 消息已读/未读 | Feature | ✅ | TestMarkRead |
| 8 | 消息撤回 | Feature | ✅ | TestRecallMessage, TestRecallMessageNotSender, TestRecalledMessageNotInHistory |

**需求完成率: 9/9 (100%)**

---

## 详细测试结果

### Server 优雅退出

| 测试项 | 结果 | 说明 |
|--------|------|------|
| Ctrl+C 后端口释放 | ✅ | `lsof -i :8080` 无 LISTEN |
| 同端口立即重启 | ✅ | 重启成功, 无 "address already in use" |
| Hub 连接清理 | ✅ | `[hub] all connections closed` 日志 |
| 数据库连接关闭 | ✅ | `db.DB().Close()` 调用 |

**代码变更:** `cmd/server/main.go` (srv.Close → srv.Shutdown + hub.Shutdown + db.Close), `internal/handler/ws.go` (Shutdown 方法)

---

### SQLite GREATEST 兼容修复

| 测试项 | 结果 | 说明 |
|--------|------|------|
| LeaveGroup 后 member_cnt 正确递减 | ✅ | TestGroupChat 通过, 无 SQLite 报错 |
| member_cnt 不会低于 0 | ✅ | MAX(cnt-1, 0) 保证 |

**代码变更:** `internal/service/chat.go:568` — `GREATEST(member_cnt - 1, 0)` → `MAX(member_cnt - 1, 0)`

---

### BUG-005: 好友上限 500 边界测试

**测试:** `TestFriendLimit500` (0.03s)

| 步骤 | 预期 | 结果 |
|------|------|------|
| 创建 499 个好友 (直接写入 DB) | count=499 | ✅ |
| 第 500 个好友: 发送请求 + 接受 | 成功, count=500 | ✅ |
| 第 501 个: 发送请求 | 成功 (pending 状态) | ✅ |
| 第 501 个: 接受请求 | ErrFriendLimit | ✅ |

---

### BUG-006: 离线好友请求持久化

**测试:** `TestOfflineFriendRequest` (0.11s)

| 步骤 | 预期 | 结果 |
|------|------|------|
| A 发送好友请求给 B (B 离线) | friends 表 status=pending | ✅ |
| B 调用 GetFriendList | 看到 A 在"待处理"分组 | ✅ |
| B 接受请求 | status=accepted, 双向记录 | ✅ |
| A 的好友列表 | 看到 B | ✅ |

---

### 修改密码

**测试:** `TestChangePassword` (0.28s), `TestChangePasswordWrongOld` (0.11s)

| 测试项 | 结果 | 说明 |
|--------|------|------|
| 旧密码正确 → 修改成功 | ✅ | 新密码 bcrypt 写入 |
| 新密码登录 | ✅ | Login 成功 |
| 旧密码登录 | ✅ | ErrAuthFailed |
| 旧密码错误 → 修改失败 | ✅ | ErrAuthFailed |
| Token 自动刷新 | ✅ | 返回新 token, 旧 session 失效 |

**客户端命令:** `/changepw <old_password> <new_password>`
**消息类型:** `MsgTypeChangePassword(400)`, `MsgTypeChangePasswordAck(401)`

---

### 黑名单

**测试:** `TestBlockUser` (0.12s), `TestBlockSelf` (0.06s), `TestUnblockUser` (0.11s), `TestGetBlacklist` (0.16s)

| 测试项 | 结果 | 说明 |
|--------|------|------|
| 拉黑用户 | ✅ | IsBlocked 返回 true |
| 重复拉黑 | ✅ | "user already blocked" |
| 拉黑自己 | ✅ | "cannot block yourself" |
| 解除拉黑 | ✅ | IsBlocked 返回 false |
| 解除未拉黑用户 | ✅ | "user not in blacklist" |
| 黑名单列表 | ✅ | 返回 2 个被拉黑用户 |
| 被拉黑者发消息 | ✅ | "you are blocked by the recipient" |

**数据表:** `blacklists` (qq, blocked_qq 唯一索引)
**客户端命令:** `/block <qq>`, `/unblock <qq>`, `/blacklist`
**消息类型:** `MsgTypeBlockUser(410)`, `MsgTypeUnblockUser(411)`, `MsgTypeBlacklist(412)`

---

### 图片/文件消息

| 测试项 | 结果 | 说明 |
|--------|------|------|
| Handler 接受 MsgTypeImage/File | ✅ | 已有 case 分支 |
| 客户端 `/sendimg` 命令 | ✅ | base64 编码发送 |
| 客户端 `/sendfile` 命令 | ✅ | base64 编码发送 |
| 文件大小限制 | ✅ | 5MB 上限 |
| 接收方自动保存 | ✅ | `DATA/<qq>/recv/<fromQQ>_<filename>` |
| 接收方显示 | ✅ | `[Image] file.jpg (1234 bytes)` |

**DTO:** `FileContent{Filename, Size, Data}`

---

### 消息已读/未读

**测试:** `TestMarkRead` (0.11s)

| 测试项 | 结果 | 说明 |
|--------|------|------|
| 新消息 ReadAt = nil | ✅ | 默认未读 |
| MarkRead 后 ReadAt != nil | ✅ | 标记已读 |
| 客户端自动发送 ReadReceipt | ✅ | 收到消息后自动发送 |

**字段:** `Message.ReadAt *time.Time`
**消息类型:** `MsgTypeReadReceipt(420)`

---

### 消息撤回

**测试:** `TestRecallMessage` (0.11s), `TestRecallMessageNotSender` (0.11s), `TestRecalledMessageNotInHistory` (0.11s)

| 测试项 | 结果 | 说明 |
|--------|------|------|
| 发送者撤回 | ✅ | IsRecalled = true |
| 非发送者撤回 | ✅ | "only sender can recall message" |
| 撤回消息从历史中过滤 | ✅ | GetHistoryWithTarget 不返回 |
| 群聊撤回通知 | ✅ | RecallNotify 广播给群成员 |
| 2 分钟限制 | ✅ | time.Since > 2min → 拒绝 |

**字段:** `Message.IsRecalled bool`
**客户端命令:** `/recall <message_id>`
**消息类型:** `MsgTypeRecall(430)`, `MsgTypeRecallNotify(431)`

---

## 代码变更统计

| 文件 | 变更类型 | 说明 |
|------|----------|------|
| `cmd/server/main.go` | 修改 | srv.Close → srv.Shutdown + hub.Shutdown + db.Close |
| `internal/handler/ws.go` | 新增+修改 | Shutdown 方法 + 4 个 handler + 黑名单检查 + read/receipt/recall handlers |
| `internal/service/chat.go` | 新增 | ChangePassword, BlockUser, UnblockUser, IsBlocked, GetBlacklist, MarkRead, RecallMessage; MAX 替换 GREATEST |
| `internal/service/chat_test.go` | 新增 | 10 个新测试 + Blacklist 加入 AutoMigrate |
| `internal/model/message.go` | 新增 | ReadAt, IsRecalled 字段 + 7 个新消息类型 + 5 个新 DTO |
| `internal/model/user.go` | 新增 | Blacklist 模型 |
| `internal/store/db.go` | 修改 | Blacklist 加入 AutoMigrate |
| `cmd/client/main.go` | 新增 | 7 个新命令 + 消息处理 + displayBlacklist |
| `cmd/client/localstore.go` | 新增 | saveReceivedFile + model import |

---

## 测试覆盖统计

| 模块 | 测试数 | 通过率 |
|------|--------|--------|
| `internal/service` | 19 | 100% |
| `cmd/client` (单元) | 1 | 100% |
| **总计** | **20** | **100%** |

### 完整测试列表

| # | 测试名 | 耗时 | 覆盖功能 |
|---|--------|------|----------|
| 1 | TestNonFriendMessageLimit | 0.10s | 非好友消息限制 |
| 2 | TestClearMessageCountsOnAcceptFriend | 0.10s | 接受好友时清除计数 |
| 3 | TestDeleteFriendThenMessageLimit | 0.10s | 删好友后恢复限制 |
| 4 | TestGetHistoryWithTarget | 0.10s | 私聊历史分页 |
| 5 | TestGroupChat | 0.15s | 群组 CRUD + GREATEST 修复 |
| 6 | TestGetGroupHistory | 0.11s | 群聊历史分页 |
| 7 | TestGroupHistoryOnlyReturnsChatMessages | 0.06s | 群聊消息过滤 |
| 8 | TestFriendLimit500 | 0.03s | BUG-005 |
| 9 | TestOfflineFriendRequest | 0.11s | BUG-006 |
| 10 | TestChangePassword | 0.28s | 修改密码 |
| 11 | TestChangePasswordWrongOld | 0.11s | 旧密码校验 |
| 12 | TestBlockUser | 0.12s | 拉黑 |
| 13 | TestBlockSelf | 0.06s | 不能拉黑自己 |
| 14 | TestUnblockUser | 0.11s | 解除拉黑 |
| 15 | TestGetBlacklist | 0.16s | 黑名单列表 |
| 16 | TestMarkRead | 0.11s | 已读标记 |
| 17 | TestRecallMessage | 0.11s | 消息撤回 |
| 18 | TestRecallMessageNotSender | 0.11s | 非发送者不能撤回 |
| 19 | TestRecalledMessageNotInHistory | 0.11s | 撤回消息过滤 |
| 20 | TestTokenFilePermission | 0.00s | Token 文件权限 |

---

## 新增消息类型汇总

| 常量 | 值 | 用途 |
|------|-----|------|
| MsgTypeChangePassword | 400 | 修改密码请求 |
| MsgTypeChangePasswordAck | 401 | 修改密码响应 |
| MsgTypeBlockUser | 410 | 拉黑用户 |
| MsgTypeUnblockUser | 411 | 解除拉黑 |
| MsgTypeBlacklist | 412 | 黑名单列表 |
| MsgTypeReadReceipt | 420 | 已读回执 |
| MsgTypeRecall | 430 | 撤回消息 |
| MsgTypeRecallNotify | 431 | 撤回通知 |

---

## 新增客户端命令

| 命令 | 说明 |
|------|------|
| `/changepw <old> <new>` | 修改密码 |
| `/block <qq>` | 拉黑用户 |
| `/unblock <qq>` | 解除拉黑 |
| `/blacklist` | 查看黑名单 |
| `/sendimg <filepath>` | 发送图片 (≤5MB) |
| `/sendfile <filepath>` | 发送文件 (≤5MB) |
| `/recall <message_id>` | 撤回消息 (2 分钟内) |

---

## 新增数据表

| 表名 | 字段 | 索引 |
|------|------|------|
| `blacklists` | id, qq, blocked_qq, created_at | UNIQUE(qq, blocked_qq) |

## 新增数据字段

| 表 | 字段 | 类型 | 说明 |
|----|------|------|------|
| `messages` | `read_at` | *time.Time | 已读时间, nil=未读 |
| `messages` | `is_recalled` | bool | 是否已撤回 |

---

## 结论

v0.7 全部 9 项需求已完成，20 个测试全部通过，构建无错误。可以进入下一阶段 (v0.8: 架构升级 & 生产就绪)。
