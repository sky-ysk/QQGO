# 下一步开发计划

**当前版本：** v0.9（已完成，已合并到 main）
**更新日期：** 2026-06-01

---

## 待完成需求（按优先级排序）

| 优先级 | 需求 | 说明 | 预估工作量 |
|--------|------|------|-----------|
| P2 | **Protobuf 协议** | 替换 JSON 序列化，降低带宽，20+ 消息类型 | 大 |
| P2 | **数据库管理接口** | `/backup` 导出 SQLite、`/clean` 清理过期消息 | 小 |
| P3 | **JWT Batch 2 实现** | Access Token + Refresh Token 双 token 认证（设计已完成） | 中 |
| P3 | **单元测试覆盖** | 核心模块测试覆盖提升 | 中 |
| P3 | **桌面端 GUI** | Wails 或 Fyne 桌面客户端 | 大 |

> **注意：** 分布式扩展（Redis PubSub）和在线状态（Redis 缓存）已在 v0.8 Batch 3 完成。

---

## 建议：v0.10 开发顺序

### 方案 A（推荐）：先小后大

1. **v0.10 Batch 1：数据库管理接口** — 工作量小，快速交付
2. **v0.10 Batch 2：JWT Batch 2 实现** — 设计已完成，只需编码
3. **v0.11：Protobuf 协议** — 改动量大，需要仔细设计和测试

### 方案 B：直接攻坚

1. **v0.10：Protobuf 协议** — 直接处理最大的架构变更

---

## 下次对话提示词 TODO：6-3日

复制以下提示词开始下一次开发：

```
继续开发，按照 docs/superpowers/plans/next-steps.md 的建议开始 v0.10
```

或者选择特定方向：

```
开始 v0.10 Batch 1：数据库管理接口（/backup 和 /clean）
```

```
开始 v0.10 Batch 2：JWT 双 token 认证实现
```

```
开始 v0.11：Protobuf 协议替换
```
